{
    "name": "oxill-forms",
    "version": "0.1.3",
    "description": "Forms for structured input in Obsidian: fill a modal, get a note.",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "node esbuild.config.mjs",
        "build": "node esbuild.config.mjs production",
        "check": "tsc --noEmit",
        "test": "node --import tsx --test src/core/*.test.ts"
    },
    "license": "MIT",
    "devDependencies": {
        "@types/node": "^22.9.0",
        "esbuild": "^0.24.0",
        "obsidian": "^1.7.2",
        "tsx": "^4.23.1",
        "typescript": "^5.6.3"
    }
}
